# import * from './generated/prisma.graphql'

type Mutation {
  createItem(
    title: String
    description: String
    price: Int
    image: String
    largeImage: String
  ): Item!

  signup(userName: String!, email: String!, password: String!): User!
}

type Query {
  allItems: [Item]!
}
